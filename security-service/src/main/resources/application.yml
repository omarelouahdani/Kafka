server:
  port: 8084
spring:
  application:
    name: auth-service
  cloud:
    discovery:
      enabled: true
  datasource:
    url: jdbc:h2:mem:user-db
eureka:
  client:
    server-url:
      defaultZone: http://localhost:8761/eureka
